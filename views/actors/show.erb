<!DOCTYPE html>
<html>
  <head>
    <title><%= @info.first["name"] %></title>
  </head>

  <body>
    <h1><%= @info.first["name"] %></h1>

      <% @info.each do |item| %>
        <b><a href="/movies/<%= item["movie_id"] %>"><%= item["title"] %></a>:</b> <%= item["character"] %><br><br>
      <% end %>
    </ul>
  </body>
</html>
